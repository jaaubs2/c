# Réglages de la version EN LIGNE (voir docs/MISE-EN-LIGNE.md).
# Copie ce fichier sous le nom « .env.production », remplis-le, puis envoie-le sur GitHub.
# Il ne contient QUE des informations publiques : elles se retrouvent de toute façon dans l'app.
# Ne mets JAMAIS ici la clé « service_role » / « secret » (la construction de l'app le refuserait).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_PUBLIC_URL=
